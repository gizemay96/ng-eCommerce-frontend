import { EqualValidationDirective } from './equal-validation.directive';

describe('EqualValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualValidationDirective();
    expect(directive).toBeTruthy();
  });
});
